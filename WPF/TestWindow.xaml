<Window x:Class="WPF.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TestWindow" Height="300" Width="300">
    <Grid>
        <DataGrid x:Name="myGrid" ItemsSource="{Binding YourItems}">
            <DataGrid.Columns>
                <DataGridTemplateColumn x:Name="userControlColumn" Header="Column1">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Label Content="Column1" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Top"/>
                                <Button Content="" Width="20"/>

                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name="firstColumn" Header="Columns2" Binding="{Binding ElementName}"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
